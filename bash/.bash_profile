# ~/.bash_profile - Login shell entry point for Git Bash
# Sources ~/.profile (PATH/env) and ~/.bashrc (prompt/aliases/functions)

[[ -f ~/.profile ]] && source ~/.profile
[[ -f ~/.bashrc ]] && source ~/.bashrc